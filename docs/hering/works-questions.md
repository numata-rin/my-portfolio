# Worksページ改修 事前確認事項

Works-details.md と現在のコードを照合し、実装前に確認が必要な項目をまとめています。

---

## 1. 各制作物のリンク情報（最重要）

現在 `profile.ts` のworksデータはGitHub・Demo URLがすべて `null` です。
以下を教えてください。

| 制作物 | GitHub URL | Demo URL |
|--------|-----------|---------|
| Study Tasks Manager | ? | ? (Vercelデプロイ済み？) |
| 麻雀点数計算アプリ | ? | 公開なし？ |
| my-portfolio | ? | ? (VercelのURL) |
| 学内カンパニー プロトタイプ | 公開可能？ | 公開可能？ |

**回答欄：**
すべて一旦nullで大丈夫です。
後からURLを自分で挿入します。


```
Study Tasks Manager
  GitHub: 
  Demo:   

麻雀点数計算アプリ
  GitHub: 
  Demo:   

my-portfolio
  GitHub: 
  Demo:   

学内カンパニー プロトタイプ
  GitHub: 
  Demo:   
```

---

## 2. my-portfolio の技術スタックの修正確認

`profile.ts` の my-portfolio の `techs` が現在 `["Python", "Django", "JavaScript", "HTML / CSS"]` になっています。
これは恐らく誤りで、実際は `["Next.js", "React", "TypeScript", "Vercel"]` などが正しいと思われますが、
**正確な技術スタックを確認させてください。**

**回答欄：**
あなたの指摘通りの技術スタックに加えて、一応、
・ClaudeCode
も追加してほしいです。

```
正しい技術スタック（使用しているものを列挙）:
  - 
  - 
  - 
  - 
```

---

## 3. 学内カンパニー プロトタイプの掲載方針

Works-details.md（セクション13）には「公開できる範囲に注意する」とあります。

**回答欄：**
今のところすべて非公開です。
学内カンパニーである商品に関連するアプリのプロトタイプを作成しているという旨だけ記載して

```
GitHubリポジトリは公開リポジトリか？
  → 

クライアント名・商品名は伏せるか？
  → 

実装済みの機能（分かる範囲で列挙）:
  - 
  - 
  - 
```

---

## 4. スクリーンショット画像の有無

各制作物のスクリーンショットを用意できますか？

- **ある** → 画像ファイルを渡していただければ `public/` に配置します
- **ない / 後で用意する** → 今回は画像なしで実装し、後から差し込める構造にします

Works-details.md では「可能であれば表示する」とあるので、なければスキップして構いません。

**回答欄：**

学内カンパニー以外あります。
それらのアプリの写真をサムネイルとして、現在のＷｏｒｋｓページの作品カードに一番左側に表示してほしいです。

画像は後から自分で用意するので、一旦placeholderのみ作ってください。
画像のサイズは今のカードの縦幅ほぼ全部で横幅は大きすぎずに適当に配置して。

```
Study Tasks Manager    : ある / ない / 後で用意する
麻雀点数計算アプリ     : ある / ない / 後で用意する
my-portfolio           : ある / ない / 後で用意する
学内カンパニー         : ある / ない / 後で用意する
```

---

## 5. Works詳細ページの要否（今回のスコープ）

Works-details.md（セクション21.3）では `/works/[slug]` の詳細ページへの拡張を想定しています。

**今回の改修で詳細ページ（`/works/[slug]`）も作りますか？**

- **A. 詳細ページも今回作る** → カードに「詳細を見る」ボタンを設置し、各作品の詳細ページへ遷移
- **B. 今回はWorksページ内に全情報を表示する** → カード内にアコーディオンなどで工夫点・改善予定も展開
- **C. 今回はWorksページ内に全情報を表示し、詳細ページは将来対応** → データ構造だけ拡張可能にしておく

Works-details.md の「完成条件」（セクション23）ではdetailページについて言及がないため、
**C（今回はページ内表示、将来に詳細ページ対応）が最もスコープに合っていると思いますが、確認させてください。**

**回答欄：**
Ａでお願いします。


```
選択（A / B / C）: 
補足があれば: 
```

---

## 6. 今後追加予定の制作物（Planned）の掲載

Works-details.md（セクション14）に「研究メモ・論文管理アプリ」「API連携版 Study Tasks Manager」が今後追加予定として記載されています。

**今回の改修でこれらを「Planned」ステータスとしてWorksページに表示しますか？**

- **表示する** → カード下部またはページ末尾に「制作予定」として軽く掲載
- **表示しない** → 今回は完成済み・進行中のものだけ掲載

**回答欄：**

表示もお願いします。

```
選択（表示する / 表示しない）: 
```

---

## 7. データ管理の場所

現在のworksデータは `app/lib/profile.ts` 内の配列で管理されています。

**改修後のデータ管理はどこに置きますか？**

- **A. `profile.ts` 内にそのまま拡張**（情報量が増えるため少し重くなります）
- **B. `app/lib/works.ts` として分離**（Works専用ファイルに切り出し、`profile.ts`は軽量に保つ）

Works-details.md（セクション15）ではデータ構造の整理を推奨しているため、
**B（専用ファイルへの分離）を推奨しますが、確認させてください。**

**回答欄：**
Ｂでお願いします。


```
選択（A / B）: 
```

---

## まとめ：最低限回答が必要な項目

改修を始めるにあたって、以下だけでも先に教えていただけると助かります。

1. 各制作物のGitHub URLとDemo URL（あるもの）
2. my-portfolioの正しい技術スタック
3. 学内カンパニープロトタイプのGitHub公開可否と実装済み機能
4. 詳細ページを今回作るか（A / B / C）
